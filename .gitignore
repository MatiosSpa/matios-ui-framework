# Build output (generated by `npm run build`, rebuilt on publish)
out/
dist/

# Dependencies (devDeps for build/test only — framework runtime stays zero-dep)
node_modules/

# Editor / OS
.DS_Store
Thumbs.db
