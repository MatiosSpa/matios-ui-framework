/* ============================================================
   MATIOS UI — matios_version.js
   Single source for the version shown in the UI + its changelog file.
   Bump this at each release (keep in sync with package.json).
   ============================================================ */
window.MATIOS_VERSION = {
  version:    '1.0.3',
  change_log: 'change_log_v103.md'
};
